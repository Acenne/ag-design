# Site images

All images are live (copied from `Supporting Images/`). To swap any image,
replace the file here keeping the same name — no code changes needed.

| Filename                       | Where it appears                                    |
| ------------------------------ | --------------------------------------------------- |
| `portfolio-hvac-site.png`      | Portfolio → Marathon redesign card                  |
| `homeowner-call.jpg`           | Services → "Conversion-first web design" card       |
| `local-map-pack.jpg`           | Services → "Local SEO & map pack" card              |
| `booking-dashboard.jpg`        | Services → "Booking & lead systems" card (navy)     |
| `analytics-report.jpg`         | Services → "Hosting & care plans" card              |
| `hvac-technician.jpg`          | "Who this is for" band (landscape rooftop shot)     |
| `hvac-technician-portrait.jpg` | Spare — not currently used (portrait Carrier shot)  |

## Tips
- Keep files ~1600px wide (JPG/WebP/PNG) so the page stays fast.
- If an image file is missing or renamed, its spot shows a labeled dashed
  placeholder instead of breaking.
